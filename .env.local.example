OPENAI_API_KEY=sk-***
WEATHER_API_BASE=https://api.open-meteo.com/v1/forecast

# Clarifying question threshold for missing preference groups (1..3). Defaults to 3 if unset.
NEXT_PUBLIC_MIN_MISSING_GROUPS=3
